image: java:8

stages:
  - test
  - build

before_script:
  - chmod +x mvnw

test:
  state: test
  script: ./mvnw test

build:
  stage: build
  script: ./mvnw package
  artifacts:
    paths:
      - target/trabalho5-1.0-SNAPSHOT.jar